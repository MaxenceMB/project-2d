using System.Collections;
using System.Collections.Generic;
using UnityEditor.ShortcutManagement;
using UnityEngine;

public class ApplicationInventory : MonoBehaviour
{
    void Start(){
        main();
    }

    void main(){/*
        Inventory inv = new Inventory();
        Item pioche = new Item("pioche", "test");
        Item fortniteBattlepass = new Item("Battle pass Fortnite", "test2");
        Item potion = new Item("Potion de soins", "test3");
        inv.AddItem(pioche);
        inv.AddItem(pioche);
        inv.AddItem(pioche);
        inv.AddItem(fortniteBattlepass);
        inv.AddItem(fortniteBattlepass);
        inv.AddItem(potion);
        inv.AddItem(potion);
        inv.AddItem(potion);
        Debug.Log(inv.ToString());
        inv.RemoveItemAt(1, 1);
        Debug.Log(inv.ToString());
        inv.RemoveItemAt(2, 3);
        Debug.Log(inv.ToString());
        inv.RemoveItemAt(1, 1);
        Debug.Log(inv.ToString());
        inv.RemoveItem(pioche);
        Debug.Log(inv.ToString());
        inv.RemoveItem(pioche, 2);
        Debug.Log(inv.ToString());*/
    }
}
